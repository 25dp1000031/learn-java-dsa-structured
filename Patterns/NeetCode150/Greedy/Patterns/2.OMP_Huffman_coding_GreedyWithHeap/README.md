🔑 2. Greedy with Priority Queue (Heap)

Idea:
Always pick the current best using a heap.

Covers:

Huffman coding
K largest/smallest decisions
Scheduling with dynamic choices

👉 If sorting alone isn’t enough and decisions change dynamically → heap